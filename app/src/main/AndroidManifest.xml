<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.appal_quranv1">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.APPAlQuranv1"
        tools:targetApi="31">

        <!-- SplashActivity sebagai aktivitas awal -->
        <activity
            android:name="com.example.appal_quranv1.ui.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity -->
        <activity
            android:name="com.example.appal_quranv1.ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Surah Detail Activity -->
        <activity
            android:name="com.example.appal_quranv1.ui.SurahDetailActivity"
            android:exported="false" />

        <!-- Juz Detail Activity -->
        <activity
            android:name="com.example.appal_quranv1.ui.JuzDetailActivity"
            android:exported="false" />
    </application>

</manifest>